#include<stdio.h>
main()
{
    int a[1000],i,k,n,count=0,l;
    scanf("%d",&n);
    scanf("%d",&k);
    for(l=0;l<n;l++)
    {
        scanf("%d",&a[l]);
    }
    for(i=0;i<n;i++)
    {
       if(a[i]<k)
       {
           count++;
       }
    }
    printf("%d",count);
}
